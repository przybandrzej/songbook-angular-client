import { CanEditSongDirective } from './can-edit-song.directive';

describe('CanEditSongDirective', () => {
  it('should create an instance', () => {
    const directive = new CanEditSongDirective();
    expect(directive).toBeTruthy();
  });
});
