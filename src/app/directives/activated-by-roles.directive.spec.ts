import { ActivatedByRolesDirective } from './activated-by-roles.directive';

describe('ActivatedByRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivatedByRolesDirective();
    expect(directive).toBeTruthy();
  });
});
